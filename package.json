{
  "name": "myportfolio",
  "version": "1.0.0",
  "description": "Portfolio with React frontend and Node.js backend",
  "scripts": {
    "dev": "node start-both.js",
    "client": "cd client && npm start",
    "server": "cd backend && npm run dev",
    "install-all": "npm install && cd client && npm install && cd ../backend && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "react-scripts": "^5.0.1"
  }
}
